{# templates/home/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<!-- HEADER -->
<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom mb-4">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('home') }}">Donkey Luminaires</a>
        <div>
            <a href="#" class="btn btn-orange me-2">Espace client</a>
            <a href="{{ path('app_login') }}" class="btn btn-outline-secondary me-2">Login</a>
            <a href="#" class="btn btn-orange">Catalogue</a>
        </div>
    </div>
</nav>

<!-- CONTENU ACCUEIL -->
<div class="container">

    <div class="row mb-4">
        <!-- CATEGORIES -->
        <div class="col-md-6 mb-3">
            <div class="p-4 rounded-4 shadow-sm bg-white">
                <h2 class="mb-3"><span style="color: orange;">🏠 Accueil</span></h2>
                <ul class="mb-2">
                    <li><strong>Catégories de luminaires :</strong></li>
                    <ul>
                        <li>Salon</li>
                        <li>Extérieur</li>
                        <li>Chambre</li>
                        <li>Bureau</li>
                    </ul>
                    <li class="mt-3"><strong>Produits en vedette</strong></li>
                    <li>
                        <strong>
                            <a href="{{ path('catalogue_tout') }}" class="btn btn-orange btn-sm mt-1">Voir tout le catalogue</a>
                        </strong>
                    </li>
                </ul>
            </div>
        </div>

        <!-- ESPACE CLIENT -->
        <div class="col-md-6 mb-3">
            <div class="p-4 rounded-4 shadow-sm bg-white">
                <h2 class="mb-3"><span style="color: orange;">👤 Espace Client</span></h2>
                <ul>
                    <li>Infos personnelles (modifiables)</li>
                    <li>Historique des commandes</li>
                    <li>Suivi des paiements</li>
                </ul>
            </div>
        </div>
    </div>

    <!-- LISTE PRODUITS -->
    <div class="row mb-4">
        {% for produit in produits|default([
            {'nom':'Lampe Design Scandinave','description':'Élégante lampe pour salon, style nordique moderne.','prix':79.99},
            {'nom':'Suspension Vintage','description':'Suspension métal esprit loft industriel.','prix':129.99},
            {'nom':'Spot LED Extérieur','description':'Spot orientable, idéal jardin ou terrasse.','prix':49.99},
            {'nom':'Veilleuse Enfant','description':'Veilleuse douce, motifs étoiles.','prix':24.90},
            {'nom':'Plafonnier Bureau','description':'Plafonnier LED puissant pour espaces de travail.','prix':89.99}
        ]) %}
        <div class="col-md-4 mb-4">
            <div class="p-3 product-card h-100 d-flex flex-column justify-content-between bg-white rounded-4 shadow-sm">
                <div>
                    <h5 class="fw-bold text-orange">{{ produit.nom }}</h5>
                    <p>{{ produit.description }}</p>
                </div>
                <div>
                    <div class="fw-bold mb-2" style="color: orange;">{{ produit.prix|number_format(2, ',', ' ') }} €</div>
                    <a href="#" class="btn btn-orange w-100">Voir le produit</a>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>

    <!-- CONNEXION / INSCRIPTION -->
    <div class="row">
        <div class="col-md-12">
            <div class="p-4 rounded-4 shadow-sm bg-white text-center">
                <h2 class="mb-3"><span style="color: orange;">🔐 Connexion / Inscription</span></h2>
                <a href="{{ path('app_login') }}" class="btn btn-outline-secondary me-2">Se connecter</a>
                <a href="{{ path('app_register') }}" class="btn btn-orange">Créer un compte</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .btn-orange { background: orange; color: #fff; border: none; }
        .btn-orange:hover { background: #e67e22; color: #fff; }
        .text-orange { color: orange; }
        body { background: #fff; }
    </style>
{% endblock %}
